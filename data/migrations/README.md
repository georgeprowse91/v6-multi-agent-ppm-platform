# Database Migrations

This directory stores schema migrations for **data**. Use a sequential naming scheme and include
both the forward migration and any rollback notes.

## Guidelines
- Keep migrations additive and backward compatible where possible
- Record data backfills in separate migration steps
- Document any required seed data updates
