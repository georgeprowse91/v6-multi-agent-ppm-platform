# Agent 03 Approval Workflow tests

This directory contains agent-level tests for the agent 03 approval workflow agent.

## Scope
- Test modules that exercise agent input validation, routing, and output formatting.
- Fixtures, helpers, and sample payloads needed for these tests.
- Any contract or environment notes required to run them locally.

## Running locally
```bash
pytest agents/core-orchestration/agent-03-approval-workflow/tests
```
