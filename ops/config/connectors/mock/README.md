# Mock connector configuration

Each file in this folder maps a production connector ID to its mock variant when `DEMO_MODE=true`.
The connector registry loads these files and applies the overrides dynamically.
