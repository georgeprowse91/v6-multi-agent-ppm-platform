# Integration Connector Configurations
# See: docs/connectors/overview.md

# Project Management Systems
planview:
  enabled: false
  base_url: "${PLANVIEW_API_URL}"
  mcp:
    server_id: "mcp_planview"
    server_url: "https://mcp.example.com/planview"
    client_id: "${PLANVIEW_MCP_CLIENT_ID}"
    client_secret: "${PLANVIEW_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      work_items: "work_items"
      resources: "resources"
  mcp_server_url: "${PLANVIEW_MCP_SERVER_URL}"
  mcp_server_id: "${PLANVIEW_MCP_SERVER_ID}"
  client_id: "${PLANVIEW_MCP_CLIENT_ID}"
  client_secret: "${PLANVIEW_MCP_CLIENT_SECRET}"
  scope: "${PLANVIEW_MCP_SCOPE}"
  tool_map:
    list_projects: "planview.listProjects"
    create_work_item: "planview.createWorkItem"
  prefer_mcp: false
  auth:
    type: "oauth2"
    token: "${PLANVIEW_API_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 30
    entities:
      - projects
      - resources
      - financials

# Clarity PPM
clarity:
  enabled: false
  base_url: "${CLARITY_INSTANCE_URL}"
  mcp:
    server_id: "mcp_clarity"
    server_url: "https://mcp.example.com/clarity"
    client_id: "${CLARITY_MCP_CLIENT_ID}"
    client_secret: "${CLARITY_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      work_items: "work_items"
  mcp_server_url: "${CLARITY_MCP_SERVER_URL}"
  mcp_server_id: "${CLARITY_MCP_SERVER_ID}"
  client_id: "${CLARITY_MCP_CLIENT_ID}"
  client_secret: "${CLARITY_MCP_CLIENT_SECRET}"
  scope: "${CLARITY_MCP_SCOPE}"
  tool_map:
    list_projects: "clarity.listProjects"
    create_work_item: "clarity.createWorkItem"
  prefer_mcp: false
  auth:
    type: "oauth2"
    token: "${CLARITY_REFRESH_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 30
    entities:
      - projects

jira:
  enabled: false
  base_url: "${JIRA_URL}"
  mcp:
    server_id: "mcp_jira"
    server_url: "https://mcp.example.com/jira"
    client_id: "${JIRA_MCP_CLIENT_ID}"
    client_secret: "${JIRA_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      issues: "work_items"
      sprints: "sprints"
  mcp_server_url: "${JIRA_MCP_SERVER_URL}"
  mcp_server_id: "${JIRA_MCP_SERVER_ID}"
  client_id: "${JIRA_MCP_CLIENT_ID}"
  client_secret: "${JIRA_MCP_CLIENT_SECRET}"
  scope: "${JIRA_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "api_token"
    email: "${JIRA_EMAIL}"
    token: "${JIRA_API_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 15
    entities:
      - projects
      - issues
      - sprints
  field_mapping:
    project_key: "project.id"
    issue_key: "task.id"
    summary: "task.name"

azure_devops:
  enabled: false
  organization: "${AZURE_DEVOPS_ORG}"
  mcp:
    server_id: "mcp_azure_devops"
    server_url: "https://mcp.example.com/azure_devops"
    client_id: "${AZURE_DEVOPS_MCP_CLIENT_ID}"
    client_secret: "${AZURE_DEVOPS_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      work_items: "work_items"
      iterations: "iterations"
  mcp_server_url: "${AZURE_DEVOPS_MCP_SERVER_URL}"
  mcp_server_id: "${AZURE_DEVOPS_MCP_SERVER_ID}"
  client_id: "${AZURE_DEVOPS_MCP_CLIENT_ID}"
  client_secret: "${AZURE_DEVOPS_MCP_CLIENT_SECRET}"
  scope: "${AZURE_DEVOPS_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "pat"
    token: "${AZURE_DEVOPS_PAT}"
  sync:
    enabled: true
    interval_minutes: 15

# Financial Systems
sap:
  enabled: false
  base_url: "${SAP_API_URL}"
  mcp:
    server_id: "mcp_sap"
    server_url: "https://mcp.example.com/sap"
    client_id: "${SAP_MCP_CLIENT_ID}"
    client_secret: "${SAP_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      cost_centers: "cost_centers"
      actual_costs: "actual_costs"
  mcp_server_url: "${SAP_MCP_SERVER_URL}"
  mcp_server_id: "${SAP_MCP_SERVER_ID}"
  client_id: "${SAP_MCP_CLIENT_ID}"
  client_secret: "${SAP_MCP_CLIENT_SECRET}"
  scope: "${SAP_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "oauth2"
  entities:
    - projects
    - cost_centers
    - actual_costs

workday:
  enabled: false
  base_url: "${WORKDAY_API_URL}"
  mcp:
    server_id: "mcp_workday"
    server_url: "https://mcp.example.com/workday"
    client_id: "${WORKDAY_MCP_CLIENT_ID}"
    client_secret: "${WORKDAY_MCP_CLIENT_SECRET}"
    tools:
      workers: "workers"
      cost_centers: "cost_centers"
      suppliers: "suppliers"
  mcp_server_url: "${WORKDAY_MCP_SERVER_URL}"
  mcp_server_id: "${WORKDAY_MCP_SERVER_ID}"
  client_id: "${WORKDAY_MCP_CLIENT_ID}"
  client_secret: "${WORKDAY_MCP_CLIENT_SECRET}"
  scope: "${WORKDAY_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "oauth2"
    client_id: "${WORKDAY_CLIENT_ID}"
    client_secret: "${WORKDAY_CLIENT_SECRET}"
  entities:
    - workers
    - cost_centers
    - suppliers

# Collaboration Tools
slack:
  enabled: false
  mcp:
    server_id: "mcp_slack"
    server_url: "https://mcp.example.com/slack"
    client_id: "${SLACK_MCP_CLIENT_ID}"
    client_secret: "${SLACK_MCP_CLIENT_SECRET}"
    tools:
      channels: "channels"
      messages: "messages"
      users: "users"
  mcp_server_url: "${SLACK_MCP_SERVER_URL}"
  mcp_server_id: "${SLACK_MCP_SERVER_ID}"
  client_id: "${SLACK_MCP_CLIENT_ID}"
  client_secret: "${SLACK_MCP_CLIENT_SECRET}"
  scope: "${SLACK_MCP_SCOPE}"
  tool_map:
    list_projects: "slack.listChannels"
    send_message: "slack.postMessage"
    list_users: "slack.listUsers"
  prefer_mcp: false
  auth:
    type: "bot_token"
    token: "${SLACK_BOT_TOKEN}"
  features:
    notifications: true
    commands: true
    interactive_messages: true

teams:
  enabled: false
  mcp:
    server_id: "mcp_teams"
    server_url: "https://mcp.example.com/teams"
    client_id: "${TEAMS_MCP_CLIENT_ID}"
    client_secret: "${TEAMS_MCP_CLIENT_SECRET}"
    tools:
      teams: "teams"
      channels: "channels"
      messages: "messages"
  mcp_server_url: "${TEAMS_MCP_SERVER_URL}"
  mcp_server_id: "${TEAMS_MCP_SERVER_ID}"
  client_id: "${TEAMS_MCP_CLIENT_ID}"
  client_secret: "${TEAMS_MCP_CLIENT_SECRET}"
  scope: "${TEAMS_MCP_SCOPE}"
  tool_map:
    list_projects: "teams.listTeams"
    send_message: "teams.postMessage"
    list_channels: "teams.listChannels"
  prefer_mcp: false
  auth:
    type: "app_credentials"
    client_id: "${TEAMS_CLIENT_ID}"
    client_secret: "${TEAMS_CLIENT_SECRET}"
    tenant_id: "${TEAMS_TENANT_ID}"
  features:
    notifications: true
    cards: true

m365:
  enabled: false
  base_url: "${M365_API_URL}"
  auth:
    type: "oauth2"
    tenant_id: "${M365_TENANT_ID}"
    client_id: "${M365_CLIENT_ID}"
    client_secret: "${M365_CLIENT_SECRET}"
    refresh_token: "${M365_REFRESH_TOKEN}"
    scopes: "${M365_SCOPES}"
    app_object_id: "${M365_APP_OBJECT_ID}"
    service_principal_object_id: "${M365_SERVICE_PRINCIPAL_OBJECT_ID}"
    application_id_uri: "${M365_APPLICATION_ID_URI}"
  workloads:
    teams: false
    exchange: false
    sharepoint: false
    planner: false
    onedrive: false
    power_bi: false
    viva: false
  features:
    use_graph_beta: false
    enable_delta_sync: false

outlook:
  enabled: false
  base_url: "${OUTLOOK_API_URL}"
  mcp:
    server_id: "mcp_outlook"
    server_url: "https://mcp.example.com/outlook"
    client_id: "${OUTLOOK_MCP_CLIENT_ID}"
    client_secret: "${OUTLOOK_MCP_CLIENT_SECRET}"
    tools:
      messages: "messages"
      calendars: "calendars"
  mcp_server_url: "${OUTLOOK_MCP_SERVER_URL}"
  mcp_server_id: "${OUTLOOK_MCP_SERVER_ID}"
  client_id: "${OUTLOOK_MCP_CLIENT_ID}"
  client_secret: "${OUTLOOK_MCP_CLIENT_SECRET}"
  scope: "${OUTLOOK_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "oauth2"
    client_id: "${OUTLOOK_CLIENT_ID}"
    client_secret: "${OUTLOOK_CLIENT_SECRET}"
    refresh_token: "${OUTLOOK_REFRESH_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 30

google_calendar:
  enabled: false
  base_url: "${GOOGLE_CALENDAR_BASE_URL}"
  mcp:
    server_id: "mcp_google_calendar"
    server_url: "https://mcp.example.com/google_calendar"
    client_id: "${GOOGLE_CALENDAR_MCP_CLIENT_ID}"
    client_secret: "${GOOGLE_CALENDAR_MCP_CLIENT_SECRET}"
    tools:
      calendars: "calendars"
      events: "events"
  mcp_server_url: "${GOOGLE_CALENDAR_MCP_SERVER_URL}"
  mcp_server_id: "${GOOGLE_CALENDAR_MCP_SERVER_ID}"
  client_id: "${GOOGLE_CALENDAR_MCP_CLIENT_ID}"
  client_secret: "${GOOGLE_CALENDAR_MCP_CLIENT_SECRET}"
  scope: "${GOOGLE_CALENDAR_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "oauth2"
    client_id: "${GOOGLE_CALENDAR_CLIENT_ID}"
    client_secret: "${GOOGLE_CALENDAR_CLIENT_SECRET}"
    refresh_token: "${GOOGLE_CALENDAR_REFRESH_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 30

smartsheet:
  enabled: false
  base_url: "${SMARTSHEET_API_URL}"
  mcp:
    server_id: "mcp_smartsheet"
    server_url: "https://mcp.example.com/smartsheet"
    client_id: "${SMARTSHEET_MCP_CLIENT_ID}"
    client_secret: "${SMARTSHEET_MCP_CLIENT_SECRET}"
    tools:
      sheets: "sheets"
      rows: "rows"
  mcp_server_url: "${SMARTSHEET_MCP_SERVER_URL}"
  mcp_server_id: "${SMARTSHEET_MCP_SERVER_ID}"
  client_id: "${SMARTSHEET_MCP_CLIENT_ID}"
  client_secret: "${SMARTSHEET_MCP_CLIENT_SECRET}"
  scope: "${SMARTSHEET_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "api_token"
    token: "${SMARTSHEET_API_TOKEN}"
  sync:
    enabled: true
    interval_minutes: 30

# Document Management
sharepoint:
  enabled: false
  site_url: "${SHAREPOINT_SITE_URL}"
  mcp:
    server_id: "mcp_sharepoint"
    server_url: "https://mcp.example.com/sharepoint"
    client_id: "${SHAREPOINT_MCP_CLIENT_ID}"
    client_secret: "${SHAREPOINT_MCP_CLIENT_SECRET}"
    tools:
      sites: "sites"
      documents: "documents"
  mcp_server_url: "${SHAREPOINT_MCP_SERVER_URL}"
  mcp_server_id: "${SHAREPOINT_MCP_SERVER_ID}"
  client_id: "${SHAREPOINT_MCP_CLIENT_ID}"
  client_secret: "${SHAREPOINT_MCP_CLIENT_SECRET}"
  scope: "${SHAREPOINT_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "azure_ad"
  document_library: "Project Documents"

# CRM & Demand Systems
salesforce:
  enabled: false
  instance_url: "${SALESFORCE_INSTANCE_URL}"
  mcp:
    server_id: "mcp_salesforce"
    server_url: "https://mcp.example.com/salesforce"
    client_id: "${SALESFORCE_MCP_CLIENT_ID}"
    client_secret: "${SALESFORCE_MCP_CLIENT_SECRET}"
    tools:
      objects: "objects"
      opportunities: "opportunities"
      cases: "cases"
  mcp_server_url: "${SALESFORCE_MCP_SERVER_URL}"
  mcp_server_id: "${SALESFORCE_MCP_SERVER_ID}"
  client_id: "${SALESFORCE_MCP_CLIENT_ID}"
  client_secret: "${SALESFORCE_MCP_CLIENT_SECRET}"
  scope: "${SALESFORCE_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "oauth2"
    client_id: "${SALESFORCE_CLIENT_ID}"
    client_secret: "${SALESFORCE_CLIENT_SECRET}"
  objects:
    - Opportunity
    - Case

# Communications & Notification Systems
azure_communication_services:
  enabled: false
  base_url: "${ACS_ENDPOINT}"
  mcp:
    server_id: "mcp_azure_communication_services"
    server_url: "https://mcp.example.com/azure_communication_services"
    client_id: "${AZURE_COMMUNICATION_SERVICES_MCP_CLIENT_ID}"
    client_secret: "${AZURE_COMMUNICATION_SERVICES_MCP_CLIENT_SECRET}"
    tools:
      sms: "sms"
      email: "email"
  mcp_server_url: "${AZURE_COMMUNICATION_SERVICES_MCP_SERVER_URL}"
  mcp_server_id: "${AZURE_COMMUNICATION_SERVICES_MCP_SERVER_ID}"
  client_id: "${AZURE_COMMUNICATION_SERVICES_MCP_CLIENT_ID}"
  client_secret: "${AZURE_COMMUNICATION_SERVICES_MCP_CLIENT_SECRET}"
  scope: "${AZURE_COMMUNICATION_SERVICES_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "api_key"
    connection_string: "${AZURE_COMMUNICATION_SERVICES_CONNECTION_STRING}"
    access_key: "${ACS_ACCESS_KEY}"
  features:
    sms: true
    email: true

twilio:
  enabled: false
  base_url: "${TWILIO_API_URL}"
  mcp:
    server_id: "mcp_twilio"
    server_url: "https://mcp.example.com/twilio"
    client_id: "${TWILIO_MCP_CLIENT_ID}"
    client_secret: "${TWILIO_MCP_CLIENT_SECRET}"
    tools:
      sms: "sms"
      calls: "calls"
  mcp_server_url: "${TWILIO_MCP_SERVER_URL}"
  mcp_server_id: "${TWILIO_MCP_SERVER_ID}"
  client_id: "${TWILIO_MCP_CLIENT_ID}"
  client_secret: "${TWILIO_MCP_CLIENT_SECRET}"
  scope: "${TWILIO_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "basic"
    account_sid: "${TWILIO_ACCOUNT_SID}"
    auth_token: "${TWILIO_AUTH_TOKEN}"
  features:
    sms: true

notification_hubs:
  enabled: false
  namespace: "${AZURE_NOTIFICATION_HUBS_NAMESPACE}"
  hub_name: "${AZURE_NOTIFICATION_HUBS_NAME}"
  mcp:
    server_id: "mcp_notification_hubs"
    server_url: "https://mcp.example.com/notification_hubs"
    client_id: "${AZURE_NOTIFICATION_HUBS_MCP_CLIENT_ID}"
    client_secret: "${AZURE_NOTIFICATION_HUBS_MCP_CLIENT_SECRET}"
    tools:
      notifications: "notifications"
      registrations: "registrations"
  mcp_server_url: "${AZURE_NOTIFICATION_HUBS_MCP_SERVER_URL}"
  mcp_server_id: "${AZURE_NOTIFICATION_HUBS_MCP_SERVER_ID}"
  client_id: "${AZURE_NOTIFICATION_HUBS_MCP_CLIENT_ID}"
  client_secret: "${AZURE_NOTIFICATION_HUBS_MCP_CLIENT_SECRET}"
  scope: "${AZURE_NOTIFICATION_HUBS_MCP_SCOPE}"
  tool_map: {}
  prefer_mcp: false
  auth:
    type: "sas"
    key_name: "${AZURE_NOTIFICATION_HUBS_SAS_KEY_NAME}"
    key: "${AZURE_NOTIFICATION_HUBS_SAS_KEY}"
  features:
    push: true

# General Integration Settings
integration_defaults:
  retry_policy:
    max_attempts: 3
    backoff_multiplier: 2
    initial_backoff_seconds: 2
  timeout_seconds: 30
  rate_limiting:
    enabled: true
    requests_per_minute: 60
  error_handling:
    log_errors: true
    alert_on_failure: true
    circuit_breaker:
      enabled: true
      failure_threshold: 5
      timeout_seconds: 60
# Asana
asana:
  enabled: false
  base_url: "${ASANA_INSTANCE_URL}"
  mcp:
    server_id: "mcp_asana"
    server_url: "https://mcp.example.com/asana"
    client_id: "${ASANA_MCP_CLIENT_ID}"
    client_secret: "${ASANA_MCP_CLIENT_SECRET}"
    tools:
      projects: "projects"
      tasks: "work_items"
      comments: "comments"
  mcp_server_url: "${ASANA_MCP_SERVER_URL}"
  mcp_server_id: "${ASANA_MCP_SERVER_ID}"
  client_id: "${ASANA_MCP_CLIENT_ID}"
  client_secret: "${ASANA_MCP_CLIENT_SECRET}"
  scope: "${ASANA_MCP_SCOPE}"
  tool_map:
    list_projects: "asana.listProjects"
    create_work_item: "asana.createTask"
    send_message: "asana.addComment"
  prefer_mcp: false
  auth:
    type: "api_token"
    token: "${ASANA_ACCESS_TOKEN}"
