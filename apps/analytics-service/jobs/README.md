# Analytics Jobs

This folder contains scheduled analytics jobs for **analytics service**. Jobs should be documented with input
sources, transformation logic, and output destinations.

Assumption: Jobs run in a batch environment (e.g., Airflow, Azure Data Factory) and are triggered
via the orchestration service.
