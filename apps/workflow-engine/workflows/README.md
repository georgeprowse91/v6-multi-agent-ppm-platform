# Workflow Definitions

This folder contains workflow definitions for **workflow engine**. Workflows describe orchestration steps
and guardrails for automation.

## Expectations
- Workflow specs (YAML/JSON)
- Diagram references or Mermaid files
- Versioned change logs for auditability
