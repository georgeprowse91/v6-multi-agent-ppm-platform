replicaCount: 2

image:
  repository: ghcr.io/georgeprowse91/document-service
  tag: "0.1.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

env:
  LOG_LEVEL: info
  PORT: "8080"
