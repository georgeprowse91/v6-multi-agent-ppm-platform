# Sandbox

This folder is used for sandbox configurations and local experiments for **connector hub**. Keep sandbox
artifacts isolated from production settings.

Assumption: Sandbox assets are safe to delete and should not be relied on for deployments.
