metadata:
  template_id: product-backlog
  title: Product Backlog
  version: 1.0.0
  status: active
  replaces: null
  replaced_by: null
  aliases:
  - product_backlog_template.md
  owner: Product
  classification: core
  composition_model: modular-v1
inputs:
- name: project_name
  type: string
  required: true
- name: prepared_by
  type: string
  required: false
- name: reporting_period
  type: string
  required: false
composition:
  order:
  - component_instance_id: core-product-backlog--data-table
    ref: ../../components/core-product-backlog--data-table.yaml
    bind:
      section_id: data-table
guidance:
  purpose: Canonical base structure for the product-backlog template.
  authoring_notes: Use concise, role-specific language. Keep each narrative block
    as 2-6 sentences and avoid embedding methodology-specific guidance in core templates.
placeholders:
- key: project_name
  token: '{{project_name}}'
  consumed_by_sections:
  - data-table
- key: backlog_item_id
  token: '{{backlog_item_id}}'
  consumed_by_sections:
  - data-table
- key: title
  token: '{{title}}'
  consumed_by_sections:
  - data-table
- key: description
  token: '{{description}}'
  consumed_by_sections:
  - data-table
- key: priority
  token: '{{priority}}'
  consumed_by_sections:
  - data-table
- key: estimate
  token: '{{estimate}}'
  consumed_by_sections:
  - data-table
- key: acceptance_criteria
  token: '{{acceptance_criteria}}'
  consumed_by_sections:
  - data-table
- key: dependencies
  token: '{{dependencies}}'
  consumed_by_sections:
  - data-table
- key: target_release
  token: '{{target_release}}'
  consumed_by_sections:
  - data-table
