metadata:
  template_id: communication-plan
  title: Communication Plan
  version: 1.0.0
  status: active
  replaces: null
  replaced_by: null
  aliases:
  - communication_plan_template.md
  owner: PMO
  classification: core
  composition_model: modular-v1
inputs:
- name: project_name
  type: string
  required: true
- name: prepared_by
  type: string
  required: false
- name: reporting_period
  type: string
  required: false
composition:
  order:
  - component_instance_id: core-communication-plan--data-table
    ref: ../../components/core-communication-plan--data-table.yaml
    bind:
      section_id: data-table
guidance:
  purpose: Canonical base structure for the communication-plan template.
  authoring_notes: Use concise, role-specific language. Keep each narrative block
    as 2-6 sentences and avoid embedding methodology-specific guidance in core templates.
placeholders:
- key: project_name
  token: '{{project_name}}'
  consumed_by_sections:
  - data-table
- key: stakeholder_group
  token: '{{stakeholder_group}}'
  consumed_by_sections:
  - data-table
- key: communication_objective
  token: '{{communication_objective}}'
  consumed_by_sections:
  - data-table
- key: channel
  token: '{{channel}}'
  consumed_by_sections:
  - data-table
- key: cadence
  token: '{{cadence}}'
  consumed_by_sections:
  - data-table
- key: sender
  token: '{{sender}}'
  consumed_by_sections:
  - data-table
- key: escalation_path
  token: '{{escalation_path}}'
  consumed_by_sections:
  - data-table
- key: success_measure
  token: '{{success_measure}}'
  consumed_by_sections:
  - data-table
