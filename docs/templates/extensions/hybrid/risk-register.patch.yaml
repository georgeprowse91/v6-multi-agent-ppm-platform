metadata:
  template_id: hybrid-risk-register-extension
  title: Hybrid Risk Register Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: hybrid
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - hybrid
  required: true
sections:
- id: extension-fields
  title: Extension Fields
  type: table
  columns:
  - name: delivery-track
    type: string
guidance:
  purpose: Patch overlay for risk-register tailored to hybrid delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: delivery_track
  token: '{{delivery_track}}'
extends: core/risk-register/manifest.yaml
add_fields:
- delivery-track
