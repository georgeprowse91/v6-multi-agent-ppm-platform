metadata:
  template_id: adaptive-sprint-review-extension
  title: Adaptive Sprint Review Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: adaptive
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - adaptive
  required: true
sections:
- id: demo-script
  title: Demo Script
  type: narrative
guidance:
  purpose: Patch overlay for sprint-review tailored to adaptive delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: demo_script
  token: '{{demo_script}}'
extends: core/sprint-review/manifest.yaml
add_sections:
- demo-script
