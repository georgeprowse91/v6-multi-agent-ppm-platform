metadata:
  template_id: agile-product-backlog-extension
  title: Agile Product Backlog Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: agile
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - agile
  required: true
sections:
- id: extension-fields
  title: Extension Fields
  type: table
  columns:
  - name: story-points
    type: string
  - name: sprint-candidate
    type: string
guidance:
  purpose: Patch overlay for product-backlog tailored to agile delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: story_points
  token: '{{story_points}}'
- key: sprint_candidate
  token: '{{sprint_candidate}}'
extends: core/product-backlog/base.yaml
add_fields:
- story-points
- sprint-candidate
