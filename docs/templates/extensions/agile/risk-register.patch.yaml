metadata:
  template_id: adaptive-risk-register-extension
  title: Adaptive Risk Register Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: adaptive
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - adaptive
  required: true
sections:
- id: extension-fields
  title: Extension Fields
  type: table
  columns:
  - name: sprint-impact
    type: string
  - name: impediment-link
    type: string
guidance:
  purpose: Patch overlay for risk-register tailored to adaptive delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: sprint_impact
  token: '{{sprint_impact}}'
- key: impediment_link
  token: '{{impediment_link}}'
extends: core/risk-register/manifest.yaml
add_fields:
- sprint-impact
- impediment-link
