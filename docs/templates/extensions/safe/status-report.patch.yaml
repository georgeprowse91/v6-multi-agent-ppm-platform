metadata:
  template_id: safe-status-report-extension
  title: Safe Status Report Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: safe
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - safe
  required: true
sections:
- id: train-level-metrics
  title: Train Level Metrics
  type: narrative
guidance:
  purpose: Patch overlay for status-report tailored to safe delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: train_level_metrics
  token: '{{train_level_metrics}}'
extends: core/status-report/base.yaml
add_sections:
- train-level-metrics
