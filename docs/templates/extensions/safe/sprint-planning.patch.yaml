metadata:
  template_id: safe-sprint-planning-extension
  title: Safe Sprint Planning Extension
  version: 1.0.0
  owner: PMO
  classification: extension
  methodology: safe
inputs:
- name: project_name
  type: string
  required: true
- name: methodology
  type: enum
  values:
  - safe
  required: true
sections:
- id: pi-objective-alignment
  title: Pi Objective Alignment
  type: narrative
guidance:
  purpose: Patch overlay for sprint-planning tailored to safe delivery.
  application: Apply this extension on top of the referenced core template to add methodology-specific content.
placeholders:
- key: pi_objective_alignment
  token: '{{pi_objective_alignment}}'
extends: core/sprint-planning/base.yaml
add_sections:
- pi-objective-alignment
