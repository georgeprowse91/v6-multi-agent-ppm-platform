component_id: core-project-management-plan--scope-baseline
version: 1.0.0
kind: core-section
title: Scope Baseline
interface:
  inputs:
  - name: scope_baseline
    source_placeholder: '{{scope_baseline}}'
    required: false
  outputs:
  - name: scope-baseline_section
    type: section
render:
  section:
    id: scope-baseline
    title: Scope Baseline
    type: narrative
    consumes_placeholders:
    - '{{scope_baseline}}'
