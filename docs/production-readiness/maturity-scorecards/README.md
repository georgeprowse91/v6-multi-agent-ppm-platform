# Maturity Scorecards

This folder stores published maturity score snapshots.

- `latest.md` is generated by `python ops/tools/collect_maturity_score.py`.
- CI should upload both `latest.md` and `artifacts/maturity/scorecard-latest.json` for each release-gate run.
- The monthly backlog in each snapshot should be copied into engineering planning as concrete improvement tickets.
