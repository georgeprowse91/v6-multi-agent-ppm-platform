# Integration Framework

Provides the shared connector integration framework with base connector classes, authentication handling, and a connector registry.

## Key files

- `framework.py` — Base integration connector, connector registry, and system-specific connector classes
- `__init__.py` — Python package marker exposing framework classes
