replicaCount: 2

image:
  repository: ghcr.io/georgeprowse91/audit-log
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

env:
  LOG_LEVEL: info
  AUDIT_WORM_CONTAINER: "audit-events"
  AUDIT_WORM_CONNECTION_STRING: ""
  AUDIT_LOG_ENCRYPTION_KEY: ""
