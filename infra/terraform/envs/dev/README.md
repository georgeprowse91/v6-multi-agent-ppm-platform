# Terraform Environments

This directory holds environment-specific Terraform configuration for **dev** deployments.
Create one folder per environment and keep shared modules under `infra/terraform/modules`.
